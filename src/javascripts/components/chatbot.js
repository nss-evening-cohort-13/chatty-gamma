const chatbots = [
  {
    bot: 'Treat Yo Self',
    keywords: ['Should I buy', 'spa', "josh grobin's fish", 'celebrity fish', 'bedazzle'],
    reply: 'Treat yo self!'
  }
];

const runChatbots = () => {
  const newMessage = $('#newMessage').val().
  if ()
}
